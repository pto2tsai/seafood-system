# 八方環球 ERP 系統 - 專案規則手冊 V12.35

## 📋 目錄

1. [專案概述](#專案概述)
2. [技術架構](#技術架構)
3. [功能模組](#功能模組)
4. [UI/UX 設計規範](#uiux-設計規範)
5. [數據結構](#數據結構)
6. [業務邏輯規則](#業務邏輯規則)
7. [響應式設計規範](#響應式設計規範)
8. [程式碼規範](#程式碼規範)
9. [測試規範](#測試規範)
10. [部署規範](#部署規範)

---

## 專案概述

### 系統名稱
**八方環球 ERP 系統 V12.35**

### 系統目標
提供一個輕量化、響應式的報價管理系統，支援：
- 快速建立報價單
- 產品搜尋與選擇
- 價格層級管理
- 歷史報價查詢
- LINE 分享功能
- 報價列印預覽

### 目標用戶
- 業務人員（建立報價）
- 管理員（系統管理、產品管理）
- 客戶（接收報價單）

### 技術棧
- **前端框架**: React 18 (CDN)
- **UI 框架**: Tailwind CSS 3
- **圖標庫**: Lucide React
- **資料庫**: Firebase Firestore
- **認證**: Firebase Authentication
- **文件類型**: 單一 HTML 文件 (Self-contained)

---

## 技術架構

### 系統架構圖

```
┌─────────────────────────────────────────┐
│           用戶界面層 (UI Layer)          │
├─────────────────────────────────────────┤
│  電腦版介面  │  手機版介面  │  列印版本  │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│        業務邏輯層 (Business Logic)       │
├─────────────────────────────────────────┤
│  報價管理  │  產品搜尋  │  價格計算     │
│  客戶管理  │  歷史記錄  │  權限控制     │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│         數據層 (Data Layer)              │
├─────────────────────────────────────────┤
│    Firebase Firestore    │  本地快取    │
│    Firebase Auth         │  Session     │
└─────────────────────────────────────────┘
```

### 頁面結構

```
App
├── LoginPage（登入頁）
├── MainPage（主頁面）
│   ├── HomePage（首頁）
│   ├── QuotePage（報價頁）
│   └── HistoryPage（歷史頁）
└── AdminPage（管理頁）
    ├── ProductManagement（產品管理）
    └── UserManagement（用戶管理）
```

### 組件層級

```
MainPage
├── Header（頂部導航 - 電腦版/手機版）
├── Sidebar（左側選單 - 僅電腦版）
├── Content（主要內容區）
│   ├── HomePage
│   ├── QuotePage
│   │   ├── CustomerInfo（客戶資訊）
│   │   ├── ProductList（產品列表）
│   │   ├── ProductSearch（產品搜尋）
│   │   └── QuoteToolbar（工具列）
│   └── HistoryPage
│       ├── SearchBar（搜尋欄）
│       └── QuoteCards（報價卡片）
└── BottomNav（底部導航 - 僅手機版）
```

---

## 功能模組

### 1. 用戶認證模組

#### 1.1 登入功能
**位置**: LoginPage

**功能描述**:
- 使用 Firebase Authentication
- 支援帳號密碼登入
- 自動記住登入狀態（localStorage）
- 登入後自動跳轉主頁面

**技術實現**:
```javascript
const handleLogin = async (email, password) => {
  const userCredential = await signInWithEmailAndPassword(auth, email, password);
  const user = userCredential.user;
  localStorage.setItem('authedUser', JSON.stringify({
    uid: user.uid,
    email: user.email
  }));
  setPage('main');
};
```

**測試用帳號**:
- 帳號: `test@test.com`
- 密碼: `test1234`

#### 1.2 登出功能
**位置**: Header（所有頁面）

**功能描述**:
- 清除 localStorage
- 登出 Firebase
- 返回登入頁

**技術實現**:
```javascript
const handleLogout = async () => {
  await signOut(auth);
  localStorage.removeItem('authedUser');
  setPage('login');
};
```

#### 1.3 權限管理
**角色類型**:
- `user`: 一般用戶（可建立報價、查看歷史）
- `admin`: 管理員（可管理產品、用戶）

**權限判斷**:
```javascript
const role = email === 'test@test.com' ? 'admin' : 'user';
```

---

### 2. 首頁模組（HomePage）

#### 2.1 快速操作卡片

**新增報價**:
- 功能: 切換到報價頁面
- 路徑: `setCurrentTab('quote')`

**歷史記錄**:
- 功能: 查看所有歷史報價
- 路徑: `setCurrentTab('history')`
- 顯示: 報價總數徽章

**產品管理** (僅管理員):
- 功能: 進入產品管理頁面
- 路徑: `setPage('admin')`

#### 2.2 近期報價列表

**顯示規則**:
- 顯示最近 5 筆報價
- 按日期降序排列
- 顯示客戶名稱、總金額、日期

**卡片內容**:
- 客戶名稱
- 價格層級（門市售價/批發價/會員價）
- 總金額
- 建立日期
- 操作按鈕（查看、編輯、刪除）

---

### 3. 報價頁模組（QuotePage）

#### 3.1 導航結構

**電腦版導航**:
```
位置: 頂部固定 (fixed top-0)
內容: [首頁][預覽][儲存][歷史][LINE] 總金額 | [管理][登出]
特點: 
  - 固定在頂部
  - 選中按鈕白底藍字
  - 顯示即時總金額
```

**手機版導航**:
```
頂部: [Logo] 八方環球 ERP [管理][登出]
底部: [首頁][預覽][儲存][歷史][LINE]
特點:
  - 頂部 sticky
  - 底部 fixed
  - 選中按鈕藍色 + 下劃線
```

#### 3.2 客戶資訊區

**桌面版**:
- 位置: 左側固定欄
- 寬度: 280px
- 樣式: 紫色漸層背景
- 內容: 客戶名稱、價格層級、備註

**手機版**:
- 位置: 頂部卡片
- 樣式: 白色背景
- 佈局: 客戶名稱 + 詳細按鈕

**欄位定義**:
```javascript
custInfo = {
  name: '',      // 客戶名稱
  tier: '門市售價', // 價格層級
  note: ''       // 備註
}
```

**價格層級選項**:
- 門市售價（預設）
- 批發價
- 會員價

#### 3.3 產品搜尋模組

**搜尋方式**:

1. **文字搜尋**:
   - 即時搜尋（onChange）
   - 搜尋範圍: 產品名稱、規格
   - 模糊匹配（toLowerCase + includes）

2. **圖片搜尋**:
   - 上傳圖片自動 OCR 識別
   - 識別文字自動填入搜尋框
   - 支援格式: JPG, PNG, WEBP

**搜尋結果顯示**:
```
產品卡片:
├── 產品圖片 (左側)
├── 產品資訊 (中間)
│   ├── 產品名稱
│   ├── 規格說明
│   └── 價格（根據層級）
└── 新增按鈕 (右側)
```

**搜尋優化**:
- 防抖處理（避免頻繁搜尋）
- 顯示搜尋結果數量
- 空狀態提示

#### 3.4 產品列表

**桌面版佈局**:
```
表格形式:
序號 | 產品名稱 | 規格 | 件數 | 單價 | 小計 | 操作
```

**手機版佈局**:
```
卡片形式:
┌─────────────────────┐
│ [1] 產品名稱         │
│     規格說明         │
│ 件數  單價  小計     │
│ [相機][複製][刪除]   │
└─────────────────────┘
```

**產品卡片結構**:
```javascript
cartItem = {
  cartId: uniqueId(),      // 唯一識別碼
  productId: product.id,   // 產品 ID
  name: product.name,      // 產品名稱
  specDetail: product.spec,// 規格說明
  qty: 1,                  // 件數
  price: product.price,    // 單價
  imageUrl: ''            // 產品圖片 URL
}
```

#### 3.5 箱容計算邏輯

**關鍵規則**:
```
件數 = 箱數 (boxes/cases)
箱容 = 每箱單位數量
總數量 = 件數 × 箱容
小計 = 件數 × 箱容 × 單價
```

**規格解析**:
```javascript
// 從規格中提取箱容資訊
function extractPackInfo(spec) {
  const patterns = [
    /(\d+)盒/,      // "12盒"
    /[xX*×](\d+)/,  // "x12", "X12", "*12", "×12"
    /(\d+)入/       // "12入"
  ];
  
  for (const pattern of patterns) {
    const match = spec.match(pattern);
    if (match) {
      return { qty: parseInt(match[1]) };
    }
  }
  return null;
}
```

**顯示格式**:
- 桌面版: `2件 × 12盒 = 24盒`
- 手機版: `2件×12=24`

**計算範例**:
```
產品: 面膜 (12盒/箱)
件數: 2
箱容: 12
單價: $280

計算:
總數量 = 2 × 12 = 24盒
小計 = 2 × 12 × $280 = $6,720
```

#### 3.6 產品操作功能

**新增產品**:
1. 搜尋產品
2. 點擊「加入」按鈕
3. 自動添加到購物車
4. 預設件數 = 1

**修改數量**:
- 方式 1: 直接輸入（支援小數）
- 方式 2: +/- 按鈕
- 最小值: 0（自動刪除）
- 輸入模式: `inputMode="decimal"`

**修改單價**:
- 直接輸入修改
- 自動重新計算小計
- 支援小數點

**上傳圖片**:
1. 點擊相機圖示
2. 選擇圖片檔案
3. 自動上傳到 Firebase Storage
4. 獲取下載 URL
5. 更新產品 imageUrl

**複製產品**:
- 複製整個產品資訊
- 生成新的 cartId
- 添加到列表末尾

**刪除產品**:
- 確認後刪除
- 從購物車移除

**重新選擇產品**:
- 點擊產品名稱
- 開啟搜尋彈窗
- 自動帶入當前產品名稱

#### 3.7 價格層級系統

**層級定義**:
```javascript
tiers = {
  '門市售價': { field: 'retailPrice' },
  '批發價': { field: 'wholesalePrice' },
  '會員價': { field: 'memberPrice' }
}
```

**價格選擇邏輯**:
```javascript
function getPrice(product, tier) {
  const field = tiers[tier].field;
  return product[field] || product.retailPrice || 0;
}
```

**價格顯示**:
- 新增產品時：根據當前層級自動選擇價格
- 切換層級：所有產品價格自動更新
- 手動修改：不受層級切換影響

#### 3.8 工具列功能

**預覽功能**:
```javascript
function printQuote() {
  window.print();
}
```

**列印樣式**:
- 隱藏導航按鈕
- 優化版面配置
- A4 紙張大小
- 包含公司資訊

**儲存功能**:
```javascript
async function saveQuote() {
  const quote = {
    customer: custInfo,
    items: cart,
    total: calculateTotal(),
    date: new Date().toISOString(),
    userId: currentUser.uid
  };
  
  await addDoc(collection(db, 'quotes'), quote);
}
```

**LINE 分享功能**:
```javascript
function shareToLine() {
  const message = `
八方環球報價單
客戶: ${custInfo.name}
總金額: $${total.toLocaleString()}
  `.trim();
  
  const url = `https://line.me/R/msg/text/?${encodeURIComponent(message)}`;
  window.open(url, '_blank');
}
```

---

### 4. 歷史頁模組（HistoryPage）

#### 4.1 搜尋與篩選

**搜尋欄位**:
- 客戶名稱
- 電話號碼
- 手機號碼
- 價格層級

**搜尋實現**:
```javascript
const filteredQuotes = quotes.filter(q => {
  if (!searchQuery) return true;
  const s = searchQuery.toLowerCase();
  return (
    q.customer.name?.toLowerCase().includes(s) ||
    q.customer.phone?.includes(s) ||
    q.customer.mobile?.includes(s) ||
    q.customer.tier?.toLowerCase().includes(s)
  );
});
```

**排序選項**:
- 依日期（預設）
- 依金額
- 依客戶

**Sticky 定位**:
- 手機版: `sticky top-0`
- 電腦版: `sticky top-24`（避開頂部導航）

#### 4.2 報價卡片顯示

**日期分組**:
```javascript
const groupedByDate = {};
filteredQuotes.forEach(quote => {
  const date = new Date(quote.date);
  const dateKey = `${date.getFullYear()}-${date.getMonth()+1}-${date.getDate()}`;
  if (!groupedByDate[dateKey]) {
    groupedByDate[dateKey] = [];
  }
  groupedByDate[dateKey].push(quote);
});
```

**日期標題**:
- 今天
- 昨天
- MM月DD日

**卡片內容**:
```
┌─────────────────────────┐
│ 🧾 客戶名稱              │
│ 💰 總金額: $12,500       │
│ 🏷️ 門市售價              │
│ 📅 2024/03/15 14:30     │
│ [查看] [編輯] [刪除]     │
└─────────────────────────┘
```

**操作按鈕**:
- 查看: 展開詳細資訊
- 編輯: 載入到報價頁面
- 刪除: 確認後刪除

---

### 5. 管理頁模組（AdminPage）

#### 5.1 產品管理

**產品列表顯示**:
- 表格形式（桌面版）
- 卡片形式（手機版）
- 支援分頁

**產品欄位**:
```javascript
product = {
  id: '',              // 產品 ID
  name: '',            // 產品名稱
  spec: '',            // 規格說明
  retailPrice: 0,      // 門市售價
  wholesalePrice: 0,   // 批發價
  memberPrice: 0,      // 會員價
  imageUrl: '',        // 產品圖片
  category: '',        // 產品類別
  stock: 0,            // 庫存數量
  createdAt: Date,     // 建立時間
  updatedAt: Date      // 更新時間
}
```

**CRUD 操作**:

**新增產品**:
```javascript
async function addProduct(product) {
  await addDoc(collection(db, 'products'), {
    ...product,
    createdAt: serverTimestamp(),
    updatedAt: serverTimestamp()
  });
}
```

**更新產品**:
```javascript
async function updateProduct(id, updates) {
  await updateDoc(doc(db, 'products', id), {
    ...updates,
    updatedAt: serverTimestamp()
  });
}
```

**刪除產品**:
```javascript
async function deleteProduct(id) {
  await deleteDoc(doc(db, 'products', id));
}
```

**批量操作**:
- 批量匯入（CSV/Excel）
- 批量刪除
- 批量調價

#### 5.2 用戶管理（待實現）

**功能規劃**:
- 用戶列表
- 權限設定
- 使用記錄

---

## UI/UX 設計規範

### 1. 色彩系統

#### 主色調
```css
/* 紫色漸層 - 主要品牌色 */
--primary-gradient: linear-gradient(135deg, #8B5CF6, #6366F1);
--purple-500: #8B5CF6;
--indigo-600: #6366F1;

/* 功能色 */
--success: #10B981;   /* 綠色 - 成功/確認 */
--warning: #F59E0B;   /* 橙色 - 警告 */
--error: #EF4444;     /* 紅色 - 錯誤/刪除 */
--info: #3B82F6;      /* 藍色 - 資訊 */

/* 中性色 */
--gray-50: #F9FAFB;
--gray-100: #F3F4F6;
--gray-200: #E5E7EB;
--gray-300: #D1D5DB;
--gray-400: #9CA3AF;
--gray-500: #6B7280;
--gray-600: #4B5563;
--gray-700: #374151;
--gray-800: #1F2937;
--gray-900: #111827;

/* 背景色 */
--bg-primary: #FFFFFF;
--bg-secondary: #F9FAFB;
--bg-accent: linear-gradient(135deg, #8B5CF6, #6366F1);
```

#### 色彩使用規則

**主色使用**:
- 頂部導航背景
- 主要操作按鈕
- 左側選單背景（桌面版）
- 重要資訊高亮

**功能色使用**:
- 綠色: LINE 分享、成功提示
- 紅色: 刪除按鈕、錯誤提示、購物車徽章
- 藍色: 歷史徽章、連結、選中狀態
- 橙色: 警告訊息

**中性色使用**:
- 文字內容（gray-700 ~ gray-900）
- 次要文字（gray-500 ~ gray-600）
- 邊框線條（gray-200 ~ gray-300）
- 背景底色（gray-50 ~ gray-100）

### 2. 字體系統

#### 字體家族
```css
font-family: 
  -apple-system, 
  BlinkMacSystemFont, 
  "Segoe UI", 
  "Noto Sans TC", 
  "Microsoft JhengHei", 
  sans-serif;
```

#### 字體大小
```css
/* 標題 */
--text-3xl: 1.875rem;  /* 30px - 主標題 */
--text-2xl: 1.5rem;    /* 24px - 次標題 */
--text-xl: 1.25rem;    /* 20px - 小標題 */
--text-lg: 1.125rem;   /* 18px - 大文字 */

/* 正文 */
--text-base: 1rem;     /* 16px - 基礎文字 */
--text-sm: 0.875rem;   /* 14px - 小文字 */
--text-xs: 0.75rem;    /* 12px - 極小文字 */
```

#### 字重
```css
--font-normal: 400;
--font-medium: 500;
--font-semibold: 600;
--font-bold: 700;
--font-black: 900;
```

#### 使用規則
- 主標題: text-2xl ~ 3xl + font-black
- 次標題: text-lg ~ xl + font-bold
- 正文: text-base + font-normal
- 說明文字: text-sm ~ xs + font-normal
- 重要數字: font-bold ~ black

### 3. 間距系統

#### Spacing Scale (基於 4px)
```css
--space-0: 0;
--space-1: 0.25rem;   /* 4px */
--space-2: 0.5rem;    /* 8px */
--space-3: 0.75rem;   /* 12px */
--space-4: 1rem;      /* 16px */
--space-5: 1.25rem;   /* 20px */
--space-6: 1.5rem;    /* 24px */
--space-8: 2rem;      /* 32px */
--space-10: 2.5rem;   /* 40px */
--space-12: 3rem;     /* 48px */
--space-16: 4rem;     /* 64px */
--space-20: 5rem;     /* 80px */
--space-24: 6rem;     /* 96px */
```

#### 使用規則
- 元素內間距: space-2 ~ 4
- 元素外間距: space-4 ~ 6
- 區塊間距: space-6 ~ 8
- 大區塊間距: space-8 ~ 12
- 頂部導航高度: space-24 (96px)

### 4. 圓角系統

```css
--rounded-none: 0;
--rounded-sm: 0.125rem;    /* 2px */
--rounded: 0.25rem;        /* 4px */
--rounded-md: 0.375rem;    /* 6px */
--rounded-lg: 0.5rem;      /* 8px */
--rounded-xl: 0.75rem;     /* 12px */
--rounded-2xl: 1rem;       /* 16px */
--rounded-3xl: 1.5rem;     /* 24px */
--rounded-full: 9999px;    /* 完全圓形 */
```

#### 使用規則
- 按鈕: rounded-lg
- 卡片: rounded-xl ~ 2xl
- 輸入框: rounded-lg
- 徽章: rounded-full
- 圖片: rounded-lg ~ xl

### 5. 陰影系統

```css
--shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
--shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1);
--shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
--shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
--shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
--shadow-2xl: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
```

#### 使用規則
- 卡片: shadow-sm ~ md
- 浮動按鈕: shadow-lg
- 彈窗: shadow-xl ~ 2xl
- 頂部導航: shadow-lg

### 6. 動畫系統

#### 過渡效果
```css
--transition-fast: 150ms;
--transition-base: 200ms;
--transition-slow: 300ms;
```

#### 動畫定義
```css
@keyframes fade-in {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes slide-up {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes slide-down {
  from {
    opacity: 0;
    transform: translateY(-10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

#### 使用規則
- 頁面切換: fade-in (300ms)
- 卡片出現: slide-up (300ms)
- 彈窗出現: slide-down (200ms)
- 按鈕 hover: transition-fast (150ms)
- 顏色變化: transition-base (200ms)

### 7. 按鈕樣式規範

#### 主要按鈕
```css
.btn-primary {
  background: linear-gradient(135deg, #8B5CF6, #6366F1);
  color: white;
  padding: 0.75rem 1.5rem;
  border-radius: 0.5rem;
  font-weight: 700;
  transition: all 150ms;
}

.btn-primary:hover {
  transform: translateY(-1px);
  box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
}
```

#### 次要按鈕
```css
.btn-secondary {
  background: white;
  color: #6366F1;
  border: 2px solid #6366F1;
  padding: 0.75rem 1.5rem;
  border-radius: 0.5rem;
  font-weight: 700;
}
```

#### 危險按鈕
```css
.btn-danger {
  background: #EF4444;
  color: white;
  padding: 0.75rem 1.5rem;
  border-radius: 0.5rem;
  font-weight: 700;
}
```

#### 按鈕尺寸
- 小按鈕: px-3 py-1.5 text-sm
- 中按鈕: px-4 py-2 text-base（預設）
- 大按鈕: px-6 py-3 text-lg

### 8. 表單樣式規範

#### 輸入框
```css
.input-base {
  width: 100%;
  padding: 0.75rem;
  border: 2px solid #E5E7EB;
  border-radius: 0.5rem;
  font-size: 1rem;
  transition: border-color 200ms;
}

.input-base:focus {
  outline: none;
  border-color: #8B5CF6;
  box-shadow: 0 0 0 3px rgba(139, 92, 246, 0.1);
}
```

#### 下拉選單
```css
.select-base {
  width: 100%;
  padding: 0.75rem;
  border: 2px solid #E5E7EB;
  border-radius: 0.5rem;
  font-size: 1rem;
  font-weight: 700;
  background-color: #FEF3C7; /* 黃色背景突出 */
}
```

#### 標籤
```css
.label-base {
  display: block;
  font-size: 0.875rem;
  font-weight: 500;
  color: #6B7280;
  margin-bottom: 0.375rem;
}
```

### 9. 卡片樣式規範

#### 基礎卡片
```css
.card-base {
  background: white;
  border-radius: 1rem;
  padding: 1.5rem;
  box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1);
  transition: transform 150ms, box-shadow 150ms;
}

.card-base:hover {
  transform: translateY(-2px);
  box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
}
```

#### 玻璃卡片
```css
.glass {
  background: rgba(255, 255, 255, 0.8);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.3);
}
```

---

## 數據結構

### 1. Firestore 集合結構

```
firestore
├── users (用戶集合)
│   └── {userId}
│       ├── email: string
│       ├── role: 'user' | 'admin'
│       ├── createdAt: timestamp
│       └── lastLogin: timestamp
│
├── products (產品集合)
│   └── {productId}
│       ├── name: string
│       ├── spec: string
│       ├── retailPrice: number
│       ├── wholesalePrice: number
│       ├── memberPrice: number
│       ├── imageUrl: string
│       ├── category: string
│       ├── stock: number
│       ├── createdAt: timestamp
│       └── updatedAt: timestamp
│
└── quotes (報價集合)
    └── {quoteId}
        ├── customer: {
        │   ├── name: string
        │   ├── tier: string
        │   ├── phone: string
        │   ├── mobile: string
        │   └── note: string
        │   }
        ├── items: [{
        │   ├── cartId: string
        │   ├── productId: string
        │   ├── name: string
        │   ├── specDetail: string
        │   ├── qty: number
        │   ├── price: number
        │   └── imageUrl: string
        │   }]
        ├── total: number
        ├── date: timestamp
        └── userId: string
```

### 2. LocalStorage 結構

```javascript
// 用戶認證資訊
localStorage.setItem('authedUser', JSON.stringify({
  uid: 'user_id',
  email: 'user@example.com'
}));

// 購物車（暫存）
localStorage.setItem('cart', JSON.stringify([...]));

// 客戶資訊（暫存）
localStorage.setItem('custInfo', JSON.stringify({...}));
```

### 3. State 管理

```javascript
// 主要狀態
const [page, setPage] = useState('login');        // 當前頁面
const [currentTab, setCurrentTab] = useState('quote'); // 當前標籤
const [cart, setCart] = useState([]);             // 購物車
const [custInfo, setCustInfo] = useState({...});  // 客戶資訊
const [products, setProducts] = useState([]);     // 產品列表
const [quotes, setQuotes] = useState([]);         // 歷史報價
const [search, setSearch] = useState('');         // 搜尋關鍵字
const [role, setRole] = useState('user');         // 用戶角色
```

---

## 業務邏輯規則

### 1. 報價計算規則

#### 小計計算
```javascript
function calculateSubtotal(item) {
  const packInfo = extractPackInfo(item.specDetail);
  
  if (packInfo) {
    // 有箱容: 件數 × 箱容 × 單價
    return item.qty * packInfo.qty * item.price;
  } else {
    // 無箱容: 件數 × 單價
    return item.qty * item.price;
  }
}
```

#### 總金額計算
```javascript
function calculateTotal() {
  return cart.reduce((sum, item) => {
    return sum + calculateSubtotal(item);
  }, 0);
}
```

#### 折扣計算（未實現）
```javascript
function applyDiscount(total, discountPercent) {
  return total * (1 - discountPercent / 100);
}
```

### 2. 價格選擇規則

```javascript
function selectPrice(product, tier) {
  const priceMap = {
    '門市售價': product.retailPrice,
    '批發價': product.wholesalePrice,
    '會員價': product.memberPrice
  };
  
  return priceMap[tier] || product.retailPrice || 0;
}
```

### 3. 庫存檢查規則（待實現）

```javascript
function checkStock(productId, quantity) {
  const product = products.find(p => p.id === productId);
  if (!product) return false;
  
  return product.stock >= quantity;
}
```

### 4. 報價編號生成

```javascript
function generateQuoteNumber() {
  const date = new Date();
  const year = date.getFullYear();
  const month = String(date.getMonth() + 1).padStart(2, '0');
  const day = String(date.getDate()).padStart(2, '0');
  const random = Math.floor(Math.random() * 10000).toString().padStart(4, '0');
  
  return `Q${year}${month}${day}${random}`;
}
```

### 5. 資料驗證規則

#### 客戶資訊驗證
```javascript
function validateCustomerInfo(custInfo) {
  if (!custInfo.name || custInfo.name.trim() === '') {
    return { valid: false, error: '請輸入客戶名稱' };
  }
  
  return { valid: true };
}
```

#### 購物車驗證
```javascript
function validateCart(cart) {
  if (cart.length === 0) {
    return { valid: false, error: '請至少新增一項產品' };
  }
  
  for (const item of cart) {
    if (item.qty <= 0) {
      return { valid: false, error: '產品數量必須大於 0' };
    }
    if (item.price < 0) {
      return { valid: false, error: '產品單價不能為負數' };
    }
  }
  
  return { valid: true };
}
```

---

## 響應式設計規範

### 1. 斷點定義

```css
/* Tailwind CSS 斷點 */
sm: 640px   /* 小型裝置 */
md: 768px   /* 平板 */
lg: 1024px  /* 筆電 */
xl: 1280px  /* 桌面 */
2xl: 1536px /* 大螢幕 */
```

### 2. 裝置判斷

```javascript
const isMobile = typeof window !== 'undefined' && window.innerWidth <= 768;
```

### 3. 響應式佈局規則

#### 導航列
```
手機版 (≤768px):
  - 頂部: sticky (Logo + 登出)
  - 底部: fixed (功能按鈕)

電腦版 (>768px):
  - 頂部: fixed (完整導航)
  - 底部: 無
  - 左側: 固定側邊欄
```

#### 產品列表
```
手機版:
  - 卡片式佈局
  - 單欄顯示
  - 垂直滾動

電腦版:
  - 表格式佈局
  - 多欄顯示
  - 固定表頭
```

#### 客戶資訊
```
手機版:
  - 頂部卡片
  - 可折疊
  - 全寬顯示

電腦版:
  - 左側固定欄
  - 280px 寬度
  - sticky 定位
```

### 4. 觸控優化

#### 按鈕尺寸
```css
/* 手機版最小觸控區域 */
.touch-target {
  min-width: 44px;
  min-height: 44px;
  padding: 12px;
}
```

#### 輸入框
```javascript
// 手機版數字鍵盤
<input 
  type="number" 
  inputMode="decimal"  // 喚起數字鍵盤
/>
```

### 5. 列印樣式

```css
@media print {
  /* 隱藏不需要列印的元素 */
  .no-print {
    display: none !important;
  }
  
  /* 調整頁面尺寸 */
  @page {
    size: A4;
    margin: 1cm;
  }
  
  /* 避免分頁斷裂 */
  .avoid-break {
    page-break-inside: avoid;
  }
}
```

---

## 程式碼規範

### 1. 命名規範

#### 變數命名
```javascript
// 使用 camelCase
const userName = 'John';
const productList = [];

// 常數使用 UPPER_SNAKE_CASE
const MAX_ITEMS = 100;
const API_KEY = 'xxx';

// 布林值使用 is/has 前綴
const isActive = true;
const hasPermission = false;
```

#### 函數命名
```javascript
// 動詞開頭
function getUser() {}
function setPrice() {}
function calculateTotal() {}
function handleClick() {}

// 事件處理器使用 handle 前綴
const handleSubmit = () => {};
const handleChange = () => {};
```

#### 組件命名
```javascript
// PascalCase
const HomePage = () => {};
const ProductCard = () => {};
const SearchBar = () => {};
```

### 2. 註解規範

```javascript
/**
 * 計算產品小計
 * @param {Object} item - 購物車項目
 * @param {number} item.qty - 數量
 * @param {number} item.price - 單價
 * @param {string} item.specDetail - 規格說明
 * @returns {number} 小計金額
 */
function calculateSubtotal(item) {
  // 提取箱容資訊
  const packInfo = extractPackInfo(item.specDetail);
  
  // 根據是否有箱容計算不同
  if (packInfo) {
    return item.qty * packInfo.qty * item.price;
  }
  
  return item.qty * item.price;
}
```

### 3. 錯誤處理

```javascript
// 使用 try-catch
async function saveQuote() {
  try {
    const docRef = await addDoc(collection(db, 'quotes'), quote);
    alert('儲存成功！');
    return docRef.id;
  } catch (error) {
    console.error('儲存失敗:', error);
    alert('儲存失敗，請稍後再試');
    return null;
  }
}

// 提前返回處理邊界情況
function processItem(item) {
  if (!item) return null;
  if (!item.qty || item.qty <= 0) return null;
  
  // 主要邏輯
  return calculateSubtotal(item);
}
```

### 4. 代碼組織

```javascript
// 1. Import 區域
import { useState, useEffect } from 'react';
import { collection, addDoc } from 'firebase/firestore';

// 2. 常數定義
const PRICE_TIERS = ['門市售價', '批發價', '會員價'];
const MAX_CART_ITEMS = 100;

// 3. 輔助函數
function calculateSubtotal(item) {
  // ...
}

// 4. 組件定義
const QuotePage = () => {
  // 4.1 State 定義
  const [cart, setCart] = useState([]);
  
  // 4.2 Effect hooks
  useEffect(() => {
    // ...
  }, []);
  
  // 4.3 事件處理函數
  const handleAddItem = () => {
    // ...
  };
  
  // 4.4 渲染
  return (
    <div>
      {/* JSX */}
    </div>
  );
};
```

### 5. React 最佳實踐

#### React 18 初始化（重要！）
```javascript
// ❌ React 17 舊寫法（已棄用）
ReactDOM.render(<App />, document.getElementById('root'));

// ✅ React 18 新寫法
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
```

#### 圖標組件使用
```javascript
// ❌ 使用未定義的圖標
<Icons.Edit className="w-4 h-4" />  // 若 Edit 未定義會報錯

// ✅ 使用前確認圖標已定義
const Icons = {
  FileText: ({className=""}) => (
    <svg className={`icon-svg ${className}`} viewBox="0 0 24 24">...</svg>
  ),
  // ... 其他圖標
};
<Icons.FileText className="w-4 h-4" />  // 正確
```

#### State 更新
```javascript
// ❌ 直接修改 state
cart.push(newItem);
setCart(cart);

// ✅ 創建新陣列
setCart([...cart, newItem]);

// ✅ 使用函數式更新
setCart(prevCart => [...prevCart, newItem]);
```

#### 條件渲染
```javascript
// ✅ 使用 && 運算符
{isAdmin && <AdminPanel />}

// ✅ 使用三元運算符
{isLoading ? <Spinner /> : <Content />}

// ✅ 使用提前返回
if (isLoading) return <Spinner />;
return <Content />;
```

#### Key 使用
```javascript
// ❌ 使用索引作為 key
{items.map((item, index) => 
  <div key={index}>{item.name}</div>
)}

// ✅ 使用唯一 ID
{items.map(item => 
  <div key={item.id}>{item.name}</div>
)}
```

---

## 測試規範

### 1. 功能測試清單

#### 登入功能
- [ ] 正確帳密可登入
- [ ] 錯誤帳密顯示錯誤
- [ ] 登入後自動跳轉
- [ ] 記住登入狀態
- [ ] 登出清除狀態

#### 報價功能
- [ ] 新增產品到購物車
- [ ] 修改產品數量
- [ ] 修改產品價格
- [ ] 刪除產品
- [ ] 複製產品
- [ ] 上傳產品圖片
- [ ] 切換價格層級
- [ ] 箱容計算正確
- [ ] 總金額計算正確

#### 搜尋功能
- [ ] 文字搜尋正常
- [ ] 圖片搜尋正常
- [ ] 搜尋結果正確
- [ ] 無結果顯示提示

#### 儲存功能
- [ ] 儲存報價成功
- [ ] 報價資料完整
- [ ] 顯示成功訊息

#### 歷史功能
- [ ] 顯示歷史報價
- [ ] 搜尋歷史報價
- [ ] 排序歷史報價
- [ ] 查看報價詳情
- [ ] 編輯歷史報價
- [ ] 刪除歷史報價

#### 列印功能
- [ ] 列印預覽正常
- [ ] 列印內容完整
- [ ] 格式符合要求

#### LINE 分享
- [ ] 開啟 LINE 應用
- [ ] 訊息內容正確
- [ ] 格式化正確

### 2. 響應式測試

#### 手機版 (≤768px)
- [ ] 頂部導航顯示正常
- [ ] 底部導航顯示正常
- [ ] 卡片佈局正常
- [ ] 觸控操作順暢
- [ ] 輸入鍵盤正確

#### 平板版 (768px ~ 1024px)
- [ ] 佈局適配正常
- [ ] 元素大小合適
- [ ] 間距舒適

#### 電腦版 (>1024px)
- [ ] 頂部導航固定
- [ ] 左側欄固定
- [ ] 表格佈局正常
- [ ] 滑鼠操作流暢

### 3. 瀏覽器兼容性測試

#### 必須支援
- [ ] Chrome (最新版)
- [ ] Safari (最新版)
- [ ] Edge (最新版)
- [ ] Firefox (最新版)

#### 行動瀏覽器
- [ ] iOS Safari
- [ ] Chrome Mobile
- [ ] Samsung Internet

### 4. 效能測試

- [ ] 首次載入時間 < 3 秒
- [ ] 頁面切換流暢 < 300ms
- [ ] 搜尋回應時間 < 500ms
- [ ] 圖片載入漸進式
- [ ] 無明顯卡頓

### 5. 安全性測試

- [ ] Firebase 規則正確設定
- [ ] 用戶權限檢查
- [ ] XSS 防護
- [ ] CSRF 防護
- [ ] 敏感資料加密

---

## 部署規範

### 1. 部署前檢查

#### 代碼檢查
- [ ] 移除所有 console.log
- [ ] 移除測試代碼
- [ ] 更新版本號
- [ ] 檢查錯誤處理
- [ ] 確認所有功能正常

#### 資源優化
- [ ] 壓縮圖片
- [ ] 移除未使用的 CSS
- [ ] 移除未使用的 JS
- [ ] 啟用快取策略

#### Firebase 設定
- [ ] Firestore 規則正確
- [ ] Storage 規則正確
- [ ] Authentication 設定
- [ ] 環境變數設定

### 2. Firebase 安全規則

#### Firestore 規則
```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 產品集合 - 所有認證用戶可讀，只有管理員可寫
    match /products/{productId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && 
                      get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }
    
    // 報價集合 - 只能訪問自己的報價
    match /quotes/{quoteId} {
      allow read: if request.auth != null && 
                     resource.data.userId == request.auth.uid;
      allow create: if request.auth != null && 
                       request.resource.data.userId == request.auth.uid;
      allow update, delete: if request.auth != null && 
                               resource.data.userId == request.auth.uid;
    }
    
    // 用戶集合 - 只能讀取自己的資料
    match /users/{userId} {
      allow read: if request.auth != null && request.auth.uid == userId;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

#### Storage 規則
```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 產品圖片 - 認證用戶可讀，管理員可寫
    match /products/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && 
                      firestore.get(/databases/(default)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }
    
    // 報價圖片 - 只能訪問自己上傳的
    match /quotes/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

### 3. 環境變數

```javascript
// Firebase 設定（生產環境）
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_AUTH_DOMAIN",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_STORAGE_BUCKET",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID"
};
```

### 4. 版本管理

#### 版本號規則
```
V主版本.次版本.修訂版本

範例: V12.33.0
- 12: 主版本（重大功能更新）
- 33: 次版本（功能新增/修改）
- 0:  修訂版本（Bug 修復）
```

#### 更新日誌
每次部署需記錄：
- 版本號
- 更新日期
- 更新內容
- 修復問題
- 已知問題

---

## 附錄

### A. 快捷鍵（規劃中）

```
Ctrl/Cmd + S : 儲存報價
Ctrl/Cmd + P : 列印預覽
Ctrl/Cmd + F : 開啟搜尋
Esc          : 關閉彈窗
```

### B. API 端點（未來擴展）

```
GET    /api/products        - 獲取產品列表
GET    /api/products/:id    - 獲取產品詳情
POST   /api/products        - 新增產品
PUT    /api/products/:id    - 更新產品
DELETE /api/products/:id    - 刪除產品

GET    /api/quotes          - 獲取報價列表
GET    /api/quotes/:id      - 獲取報價詳情
POST   /api/quotes          - 新增報價
PUT    /api/quotes/:id      - 更新報價
DELETE /api/quotes/:id      - 刪除報價
```

### C. 瀏覽器支援

```
Chrome   : ≥90
Safari   : ≥14
Firefox  : ≥88
Edge     : ≥90
```

### D. 效能指標

```
FCP (First Contentful Paint)  : < 1.5s
LCP (Largest Contentful Paint): < 2.5s
FID (First Input Delay)       : < 100ms
CLS (Cumulative Layout Shift) : < 0.1
```

### E. 常見問題 FAQ

**Q: 如何重置密碼？**
A: 聯繫管理員重置

**Q: 報價可以儲存多久？**
A: 永久儲存，除非手動刪除

**Q: 可以匯出報價嗎？**
A: 使用列印功能，可儲存為 PDF

**Q: 如何批量匯入產品？**
A: 管理頁面 > 產品管理 > 批量匯入

**Q: LINE 分享失敗怎麼辦？**
A: 確認已安裝 LINE 應用，或複製連結手動分享

---

## 版本歷史

### V12.35 (2024-12-15)
- ✅ 修復手機版件數/數量輸入欄位無法輸入問題
- ✅ 將所有數字輸入改為 `type="text"` + `inputMode="decimal"` + `pattern="[0-9]*"`
- ✅ 修改客戶價格等級預設值從「門市售價」改為「整件價」
- ✅ 同時修復單價輸入欄位的手機相容性

### V12.34 (2024-12-15)
- ✅ 修復 React 18 相容性問題（ReactDOM.render → createRoot）
- ✅ 修復 Icons.Edit 未定義錯誤（改用 Icons.FileText）
- ✅ 統一所有歷史按鈕行為（setShowHistory → setCurrentTab('history')）
- ✅ 手機版與電腦版歷史功能現在一致跳轉到歷史頁面
- ✅ 修復共 7 處歷史按鈕：
  - BottomNav 底部導航
  - 首頁「歷史記錄」按鈕
  - 首頁「查看全部」按鈕
  - 電腦版頂部導航（首頁、歷史頁、報價頁）
  - 電腦版左側欄「歷史報價」按鈕

### V12.33 (2024-12-15)
- ✅ 修復手機版歷史按鈕功能
- ✅ 為所有頁面添加手機版頂部導航
- ✅ 完善響應式設計

### V12.32 (2024-12-15)
- ✅ 修復電腦版歷史按鈕功能
- ✅ 調整 HistoryPage sticky 位置
- ✅ 為所有頁面添加電腦版頂部導航

### V12.31 (2024-12-15)
- ✅ 電腦版頂部添加歷史按鈕
- ✅ 手機版底部添加歷史按鈕
- ✅ 統一雙平台導航配置

### V12.30 (2024-12-15)
- ✅ 修復手機版件數輸入問題
- ✅ 添加 inputMode="decimal" 屬性
- ✅ 優化數字鍵盤體驗

### V12.29 (2024-12-15)
- ✅ 電腦版功能列整合到頂部
- ✅ 頂部導航固定顯示
- ✅ 釋放底部空間

### V12.26-V12.28
- ✅ 箱容計算邏輯修正
- ✅ 底部導航統一
- ✅ 底部導航簡化

### V12.0-V12.25
- ✅ 核心功能開發
- ✅ Firebase 整合
- ✅ 響應式設計
- ✅ 基礎功能完善

---

## 維護聯絡

**開發團隊**: Claude + 用戶協作開發
**最後更新**: 2024-12-15
**文件版本**: V12.35

---

**文件結束**
