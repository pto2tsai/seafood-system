# 八方環球 ERP — V20.0 ～ V20.9 更新說明

> **文件版本**：對應系統 V20.9
> **更新日期**：2026-08-19
> **前一份文件**：《八方環球 ERP V19 系統手冊》

---

## 目錄

1. [本次更新總覽](#1-本次更新總覽)
2. [箱容權威順序重整（核心）](#2-箱容權威順序重整核心)
3. [領料明細的重量欄位](#3-領料明細的重量欄位)
4. [不定重品項的雙單位評估](#4-不定重品項的雙單位評估)
5. [庫存健康度的重新定位與移除](#5-庫存健康度的重新定位與移除)
6. [產品分類判斷收斂](#6-產品分類判斷收斂)
7. [調撥模組調整](#7-調撥模組調整)
8. [UI 一致性](#8-ui-一致性)
9. [版本更新記錄](#9-版本更新記錄)
10. [待處理事項](#10-待處理事項)

---

## 1. 本次更新總覽

| 類別 | 主要內容 |
|------|----------|
| 🔴 資料正確性 | 箱容主檔的 0／1 視為未設定、新增領料實算來源 |
| 🆕 新資料 | 領料明細的重量欄位、不定重品的平均件重 |
| 🗑 移除 | 庫存健康度整頁（功能由調撥建議與採購計劃承接） |
| 🧹 收斂 | 產品分類判斷 4 套 → 1 套 |
| 📐 UI | 分析中心導航列補齊、調撥圖示加文字、物流新增選項 |

**本次最重要的變更**：發現箱容主檔對原料品項大量填 0 或 1（＝未設定），導致換算失效；改以領料實算補上，並修正判定規則。

---

## 2. 箱容權威順序重整（核心）

### 2.1 問題發現

以領料明細交叉驗證箱容主檔，36 個品號中**不一致 12 筆**：

| 品號 | 領料實算 | 主檔 | 品名 |
|------|----------|------|------|
| D6660200 | **15.36 KG** | 1 KG | 船凍透抽 2號 |
| D6660300 | **13.5 KG** | 1 KG | 船凍透抽 3號 |
| D6744000 | **17.85 KG** | **0 KG** | 魷魚4/6 |
| D6763400 | **17.96 KG** | 0 KG | 魷魚身TUBE 300/400 |
| C63013 | **29.57 KG** | 1 KG | 土魠魚原料 |

主檔中換算率為 **0 的有 882 筆**、為 **1 的有 456 筆**，其中單位為 KG 者 114 筆——這些幾乎都是「不定重」品項的佔位值，不是真實箱容。

### 2.2 「1」的判定要看單位

不能一律把 1 視為未設定：

| 狀況 | 筆數 | 判定 |
|------|------|------|
| 換算率 0 | 882 | **必定未設定** |
| 換算率 1、單位「件」 | 12 | **合理**（如秋刀魚 特號*10KG，整件計價） |
| 換算率 1、單位 KG | 114 | **可疑**，往下找其他來源 |
| 換算率 1、其他單位（張／包／組／箱） | 330 | 多數合理，予以採信 |

```javascript
const pmUnitIsWeight = /^(KG|kg|Kg|K|公斤|斤)$/.test(String(pm?.unit || '').trim());
const pmUsable = pm && pm.qty > 0 && !(pm.qty === 1 && pmUnitIsWeight);
```

### 2.3 新的權威順序

```
箱容主檔（有效值）    ← source: 'master'
  ↓ 0 或「1＋重量單位」
領料實算              ← source: 'usage'  🆕
  ↓
報價表箱容欄          ← source: 'quote'
  ↓
實際成交推導          ← source: 'sales'
  ↓
規格字串解析          ← source: 'spec'，不採用於換算
```

```javascript
const PACK_TRUSTED = ['master', 'usage', 'quote', 'sales'];
```

### 2.4 修正效果

| 品項 | 原本箱容 | 現在 |
|------|----------|------|
| 船凍透抽 2號 | 1（失效） | **15.36** |
| 魷魚4/6 | 0（失效） | **17.85** |
| 土魠魚原料 | 1（失效） | **29.57** |
| 秋刀魚 特號 | 1 件 | **1**（正確保留） |

**影響範圍**：報價單小計、庫存健康、品項速查的件數、補貨建議——全部因 `resolvePack` 是單一入口而同步變準。

---

## 3. 領料明細的重量欄位

### 3.1 問題

鼎新「部門異動單據統計表」的欄位：

```
單據名稱 / 部門名稱 / 品號 / 品名 / 規格 / 異動包裝數量 / 包裝單位 / 異動數量 / 單位
                                        ↑ 件（原本有抓）  ↑ KG（原本丟棄）
```

系統只解析件數，**重量資料進來就被丟棄**——與當初報價表箱容欄被丟棄是同樣的情況。

### 3.2 修正

匯入時新增 `baseQty`（基本單位數量，多為 KG）與 `baseUnit` 欄位。

### 3.3 領料實算的計算

```javascript
const packFromUsage = React.useMemo(() => {
  // 依品號累加件數與 KG，相除得每件實際重量
  // 只採 KG 單位、樣本需 ≥2 筆、結果須介於 0～500
}, [materialUsageDetails]);
```

**實測驗證**（2026 年 7 月領料，40 筆）：

```
A5021212  502白仁 100/200*12KG      30 件 /  360.0 KG  → 每件 12.00
A5021214  502白仁 100/200*1.8KG*8塊 187 件 / 2692.8 KG → 每件 14.40
合計 30,246 件 / 478,059 KG
```

數值皆為整數（12.00、14.40、18.00），資料乾淨。

> 領料實算對**原料**最準（魷魚、透抽、土魠），因為那正是主檔填 0 或 1 的族群。
> **需先匯入領料明細**，此來源才會生效。

---

## 4. 不定重品項的雙單位評估

### 4.1 原則

不定重品**不做單位換算，改以雙單位（件／KG）並列**，較為客觀。

### 4.2 平均件重的來源

`salesDaily` 同時有 `qty`（不定重品即公斤數）與 `pk`（件數），相除即平均每件重量：

```javascript
const avgWeightPerPiece = React.useMemo(() => {
  // 依品號累加，取平均；保留樣本數與最輕最重供判讀
  // 樣本 <3 筆不採用；單件重量 >1000 視為異常
}, [dailySales]);
```

回傳 `{ avg: 12.4, n: 87, min: 9.8, max: 15.2 }`。

**與箱容推導的差異**：`packFromSales` 要求眾數佔六成以上（定重品應一致），不定重每件重量本就不同、一致性必然偏低而被排除，故改取平均值。

### 4.3 庫存健康度的不定重處理

判定方式：箱容 1 且計價單位為重量。

| 項目 | 處理 |
|------|------|
| 可售天數 | **不計算**，顯示「不定重」 |
| 等級判斷 | 改以安全庫存水位（缺貨／低於半量／低於安全量） |
| 月銷 | 標出實際單位（KG） |
| 進度細線 | 不繪製 |

原因：庫存以「件」計、銷量以「KG」計，兩者相除無意義。

---

## 5. 庫存健康度的重新定位與移除

### 5.1 為什麼砍掉

釐清「這一頁想讓我知道什麼」後發現，它混了三件時間尺度完全不同的事：

| 問題 | 誰做 | 頻率 | 已經有誰在做 |
|------|------|------|--------------|
| 今天要調什麼回工廠 | 倉管會計 | 每天 | **調撥的自動建議** |
| 這個月要採購什麼 | 老闆 | 每月 | **採購計劃** |
| 哪些貨賣不動、快過期 | 老闆 | 每季 | 尚無（效期風險的題目） |

而且三者的「正常」定義互相矛盾——熱銷品工廠倉只有 2 天量，對第一件事是「該調了」，對第二件事是「完全正常」。

**與調撥自動建議的功能對照**：

| 項目 | 庫存健康 | 調撥自動建議 |
|------|----------|--------------|
| 觸發門檻 | 前置期 2 天（寫死） | `triggerDays: 3`（可設定） |
| 補到幾天 | 無 | `targetDays: 7` |
| 在途扣除 | ❌ | ✅ 近 3 天已開單不重複建議 |
| 28 法則主力判定 | ❌ | ✅ |
| 效期預警 | ❌ | ✅ `expAlertDays: 90` |
| 可直接開單 | ❌ | ✅ |

庫存健康度做的每一件事都有別人做得更完整，留著只多一份會漂移的邏輯。

### 5.2 移除內容

- `RestockPage` 元件（216 行）
- 分析中心的選單項與渲染分支
- 頁首標題與說明

**保留** `cachedStockHealth` 資料本身——仍餵給儀表板警報卡片與一份報表。

儀表板卡片改為「庫存警示／點入採購計劃」，點擊導向採購計劃。

### 5.3 過程中做過但已隨頁面移除的調整

以下為砍除前的改版，記錄供參考：

- 依「今天能做什麼」分組（該開調撥單／該採購或生產／先觀察）
- 兩層判定：工廠倉 vs 前置期、工廠倉＋外倉 vs 14 天
- 調撥前置期會依星期計算（週五開單算 3 天，週一才到）
- 每組預設 50 筆、可整組展開
- 排序固定月銷高→低

**其中「工廠倉只放週轉量、主要庫存寄外倉」這個業務事實**，未來若重做採購或調撥相關功能仍須納入——工廠倉可售天數短是正常運作，不是缺貨。

---

## 6. 產品分類判斷收斂

### 6.1 問題

「成品／原料」判斷有 **4 套實作**，回傳值與讀取節點都不同：

| 位置 | 回傳值 | 讀哪個節點 |
|------|--------|------------|
| 6024 | `finished`／`raw`（英文） | `productCategories` |
| 7778 | `成品`／`原料`（中文） | `productCategories` ＋ 舊節點 |
| 8492 | `成品`／`原料` | **只讀已棄用的 `rawMaterialProducts`** |
| 8563 | `成品`／`原料` | `productCategoriesMap` |

第三套完全讀錯節點——與先前庫存健康度原料標記失效是同一個 bug 的第二份拷貝。

另有一套自動判斷多比對 `otherStock`，其他三套沒有，導致同一品項在不同頁面可能被歸到不同分類。

### 6.2 收斂

```javascript
const resolveCategory = (pid, stocks, isStoreOnly, lang = 'zh') => { … }
```

`lang` 參數決定回傳中文或英文，因兩邊呼叫端本就期待不同格式。

**驗證**：八種情境（手動設定、舊節點、三種倉別分佈、門市專賣、無庫存）中英文對應一致。

---

## 7. 調撥模組調整

| 項目 | 說明 |
|------|------|
| 物流新增「崇文司機」 | 備註帶「崇文司機取貨回嘉義」，排在第一 |
| 品項列圖示加文字 | 電腦版顯示「✎ 備註」「✕ 刪除」（數量直接在輸入框改） |
| LINE 分享錯誤提示 | 原本只顯示「分享失敗」，現在顯示實際錯誤並提示改用「存圖片」 |
| 產圖元件防護 | `html2canvas` 由 CDN 載入，未載入成功時給明確訊息 |

> **載入修改**功能位於「📋 紀錄」內，非已挑清單。電腦版與手機版共用同一段程式。

---

## 8. UI 一致性

### 8.1 分析中心導航列

原本缺少「調撥」與「分析」兩顆，與其他分頁不一致。已補齊為：

```
客戶 · 儀表板 · 報價 · 調撥 · 分析 · 管理 · 登出
```

「分析」呈現白底反白表示目前位置；「調撥」會順帶觸發批號載入。

> 分析中心使用自己的一套導航，非共用的 `DesktopNav`。本次僅補齊按鈕，未整併——其頁首尚須兼顧子頁標題與返回鈕，直接替換風險較高。

### 8.2 其他

- 報價底部移除「總金額」字樣（`$` 已足以辨識）
- 分析中心頁首內距 `py-4` → `py-3 md:py-4`，手機版與其他分頁齊平

---

## 9. 版本更新記錄

| 版本 | 內容 |
|------|------|
| V20.0 | **產品分類判斷收斂為單一入口**（4 套 → 1 套） |
| V20.1 | 不定重品項不計算可售天數；補上 `cachedStockHealth` 遺漏的依賴 |
| V20.2 | 庫存健康度改為每組預設 50 筆、整組展開 |
| V20.3 | 兩層判定（工廠倉 vs 前置期、總量 vs 14 天）、前置期依星期計算 |
| V20.4 | **移除庫存健康度整頁**，儀表板卡片改導向採購計劃 |
| V20.5 | 物流新增崇文司機、圖示加文字、LINE 分享錯誤提示 |
| V20.6 | 不定重品的平均件重（由銷貨日報反推） |
| V20.7 | **領料明細補上重量欄位**（`baseQty`／`baseUnit`） |
| V20.8 | 分析中心導航列補齊調撥與分析 |
| V20.9 | **箱容權威順序重整**：主檔 0／1 判定 ＋ 領料實算新來源 |

---

## 10. 待處理事項

### 10.1 待驗證

| 項目 | 說明 |
|------|------|
| **領料明細匯入** | 領料實算來源需先匯入才生效；目前僅確認 7 月一份 |
| 箱容主檔涵蓋率 | 匯入後卡片顯示的數字 |
| `auditSalesDaily` | 稽核腳本尚未執行 |
| 品項主檔 | 尚未匯入 |
| 未結案應收總額 | 應為 33,775,041 |
| **採購計劃** | 產區是否已設定、建議數量是否合理 |

### 10.2 採購計劃待修

**目前的建議量算法有誤**：

```javascript
const leadTime = getLeadTime(pid);          // 有算，但沒用到
const suggestedQty = Math.max(0, Math.ceil(safety - totalStock));
```

建議量是「安全庫存 − 現有庫存」，**完全沒用到前置期與目標庫存天數**。產區設定（厄瓜多 45 天、挪威 65 天等 15 個產區）算出來就丟棄。安全庫存預設值 40 件，對月銷 900 件的品項毫無意義。

**更根本的問題**：現行模型是「持續補貨」，但實際採購型態是**批次進口**：

| 品項 | 採購週期 |
|------|----------|
| 白蝦 | 5–6 個月 |
| 魷魚 | 一年 |
| 鮭魚 | 一年 |
| 船凍透抽 | 2–3 個月 |

國內採購立即下單、立即到貨，不需要系統規劃。真正需要輔助的是進口批次——**一次決定半年到一年的量**，算錯會壓住資金或缺貨數月。

採購量的基礎為**加工領料量或銷售量**。

### 10.3 採購對照表（規劃中）

定位為「把要查的數字一次擺好」，而非替使用者決定數量——市場行情、匯率、產地捕撈狀況、資金配置等關鍵因素系統並不知道。

規劃呈現：

```
魷魚 3/4
去年消耗    12,400 件 ≈ 153,800 KG（領料 9,800 + 直接銷售 2,600）
今年至今     8,100 件（8 個月，年化 12,150）
現有庫存     3,200 件 ≈ 39,700 KG（工廠 200 + 外倉 3,000）
可撐         3.2 個月
```

**待確認**：放在既有的採購計劃頁或另開、品項群以關鍵字抓取或可設定清單。

### 10.4 仍需收斂

| 項目 | 現況 |
|------|------|
| `isMobile` | 定義 8 次（內容相同），渲染時求值，視窗縮放不重算 |
| `getUnifiedProducts` | 只被呼叫 1 次，形同虛設 |
| 22 個彈窗 | 各自管理開關與關閉邏輯 |
| 分析中心導航 | 未整併至 `DesktopNav` |

### 10.5 待討論

| 項目 | 說明 |
|------|------|
| **效期風險** | 唯一無人負責的問題。可回答「這批貨在過期前賣得完嗎」——`賣完天數 > 剩餘效期` 即為風險，依損失金額排序。與調撥既有的效期預警分工：調撥管「快到期趕快調回來出」，此處管「根本賣不完該處理掉」。限制：`batchStock` 主要涵蓋白蝦、鮭魚、魷魚、黑螺肉 |
| `salesDaily` 歷史回填 | 需提供現行每日同步 GAS，解析邏輯必須一致 |
| 客戶品項覆蓋表 | LINE 確認的品號需與 ERP 分開儲存 |

### 10.6 架構債

**22,000 行、228 個 useState 在同一個元件。** 這是所有重複問題的溫床。真正的解法是拆檔案，但等同重寫，風險過高，現階段不建議。

---

*本文件由 Claude AI 協助生成，版權歸八方環球所有。*
