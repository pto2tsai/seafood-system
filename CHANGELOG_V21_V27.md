# 八方環球 ERP — V21.0 ～ V27.0 檢討與更新紀錄

> **文件版本**：對應系統 V27.0
> **期間**：2026-08-19 ～ 2026-08-20
> **前一份文件**：《V20.0 ～ V20.9 更新說明》

---

## 目錄

1. [這一輪做了什麼](#1-這一輪做了什麼)
2. [調撥單傳真改造（V21.0–V24.0）](#2-調撥單傳真改造)
3. [報表解析的系統性錯誤（V24.1–V24.5）](#3-報表解析的系統性錯誤)
4. [分析中心重整（V24.6–V26.6）](#4-分析中心重整)
5. [實測回報修正（V26.7–V27.0）](#5-實測回報修正)
6. [檢討：這一輪暴露的問題](#6-檢討這一輪暴露的問題)
7. [版本更新記錄](#7-版本更新記錄)
8. [待處理事項](#8-待處理事項)

---

## 1. 這一輪做了什麼

| 類別 | 主要內容 |
|------|----------|
| 🔴 資料正確性 | 庫存周轉月銷高估 20 倍、報表欄位被丟棄（3 支解析器）、箱容稽核容差過嚴 |
| 📠 調撥單 | 傳真辨識度全面改造：字型、二值化、PDF 不壓縮、動態頁面尺寸 |
| 📊 分析中心 | 八頁視覺統一、資料來源標示、季節性品項處理 |
| 🧹 收斂 | 價格正規化 4 處合 1、三支銷貨解析器對齊、批次匯入接回 |
| 🐛 實測修正 | 存圖片無反應、PDF 留白、搜尋列被鍵盤推走、彈窗被導航列遮蔽 |

**程式規模**：22,032 行、220 個 useState、22 個彈窗（單一元件）。

---

## 2. 調撥單傳真改造

凍庫反映傳真「字太淡、看不清楚」。追查後發現問題不在單一環節，而是**整條路徑每一站都在損耗**。

### 2.1 五個損耗點

| 環節 | 問題 | 修正 | 版本 |
|------|------|------|------|
| 字型 | 模板指定 `Noto Sans TC` 卻從未載入，實際 fallback 到蘋方（筆畫細） | 真的載入，並等 `document.fonts.ready` | V21.7 |
| 抗鋸齒 | 灰階邊緣在傳真二值化時變成雜點 | 關閉 `WebkitFontSmoothing` ＋ 自行二值化 | V21.7 / V22.6 |
| 產圖倍率 | 寫死 `scale: 4` → 4160px，**超過 iOS canvas 上限 4096px**，靜默縮小 | 依 B5 實際尺寸回推 300 DPI，並受裝置上限約束 | V22.5 / V22.7 |
| 圖片壓縮 | JPEG 品質 0.92 在黑白線稿產生壓縮雜訊 | 改 PNG 無損 | V21.6 |
| **LINE 傳輸** | **傳「圖片」會強制重新壓縮（約 1000px）**，4160px 被壓掉四分之三 | 改傳 PDF，LINE 視為檔案不處理 | V22.4 |

> 前四項改完效果都有限，因為**第五項把成果全部抵消**。這是這次最重要的發現——
> 不管產圖多精細，只要經過 LINE 的圖片壓縮就會被打回原形。

### 2.2 版面演進

橫式 → 直式 → 橫式，來回改了兩次。

| 版本 | 決定 | 理由 |
|------|------|------|
| V21.4 | 改直式 | 傳真紙為直式，橫式內容會被旋轉縮小 |
| V21.8 | **改回橫式** | 橫式寬 1040px 比直式多 246px，欄位不必折行，字才放得大 |

**最終規格**：橫式 1040×734（比例 1.417，貼近 A4 橫式 1.414）、四欄（品名/規格/批號合併為一欄）、等比字級階梯。

### 2.3 字級階梯

先前逐項微調造成十三種零散字級，表頭 27px 甚至小於內文 28px。收斂為比例 1.25 的階梯：

```
25  序號
31  品名、表頭、備註、頁尾、聯絡人
39  規格、批號、標題
49  數量
61  倉名
```

框線同步分三級：內框 2px、表頭 3px、主分隔 4px。

### 2.4 字重的反直覺結論

**字重 900 對中文有害**。「撥」「號」「蝦」筆畫多，在傳真的低解析度下筆畫會黏成黑塊。
正確做法是**字級加大、字重降到 700–800、加字距**。

---

## 3. 報表解析的系統性錯誤

### 3.1 報表本來就有的欄位被丟棄

同一種錯誤在三支解析器重複發生：

| 報表 | 被丟棄的欄位 | 後果 | 版本 |
|------|------------|------|------|
| 商品銷貨期報表 | 銷貨包裝數量（件） | 件數必須用「數量 ÷ 箱容」推導 | V24.1 |
| 領料明細 | 異動數量（KG） | 無法得知每件實際重量 | V24.2 |
| 銷貨明細 | 銷貨包裝數量（件） | 件數遺失 | V24.3 |

**根本反省**：先前花了數天建立「主檔 → 領料實算 → 報價表 → 成交推導 → 規格解析」五層箱容權威順序，就為了推算件數——**但報表本身就直接提供件數**。整套推導是繞遠路，而且因箱容不準而失真。

V24.1 起，箱容從「主要手段」降為「舊資料的備援」。

### 3.2 同一份報表有兩支解析器

| 目標節點 | 解析器 A | 解析器 B | 狀態 |
|---------|---------|---------|------|
| `materialUsageByMonth` | `handleMaterialUsageUpload` | `processMaterialUpload` | V24.2 對齊 |
| `salesByMonth` | `handleMonthlySalesUpload` | `processSalesUpload` ＋ `handleBatchSalesUpload` | V24.3–4 對齊 |

**走哪個入口決定資料完不完整。** 這解釋了「領料實算是 0」的現象。

### 3.3 危險的欄位比對寫法

```javascript
// ❌ '異動' 同時匹配「異動包裝數量」與「異動數量」，後者覆蓋前者，
//    存入的究竟是件數還是重量取決於欄位順序
if (c === '數量' || c.includes('異動') || c.includes('包裝數量')) colMap.qty = idx;

// ✅ 完全比對、分開存放
if (c === '異動包裝數量' || c === '包裝數量') colMap.qty = idx;      // 件
if (c === '異動數量' || c === '數量') colMap.baseQty = idx;          // KG
```

**中文欄位名互相包含的情形很常見，`includes` 用於欄位比對是不安全的。**

### 3.4 批次匯入功能是斷的

「批次匯入銷貨明細」的解析器、彈窗、寫入邏輯都完整，**但沒有任何 UI 元素會觸發它**。等於死掉的功能。V24.4 接回入口並補上件數欄位。

---

## 4. 分析中心重整

### 4.1 庫存周轉的三個錯誤

這一頁交付後才被使用者發現數字錯誤，是這一輪最嚴重的品質問題。

**(a) 月銷高估 20 倍**（V24.8）

```javascript
const threeMonthsAgo = new Date();
threeMonthsAgo.setMonth(threeMonthsAgo.getMonth() - 3);   // 算了，但從未用來過濾
Object.entries(salesPeriodByMonth).forEach(...)           // 加總「全部月份」
const monthlyQty = sales.qty / 3;                          // 卻除以 3
```

十二個月資料 → 4 倍；加上 `d.qty` 是**盒數**而庫存是**件數**（一件 12 盒）→ 再 12 倍。
**而程式註解還寫著「銷貨包裝數量（件）」，註解本身是錯的。**

**(b) 庫存與報價頁差 46 件**（V24.8）

報價頁排除 `EXCLUDED_WAREHOUSES`（遺失庫、退貨庫、耗材庫），庫存周轉沒排除。

**(c) 顯示「沒賣」但有月銷**（V26.1）

`999` 同時被當成「沒賣」的標記與天數上限，但真實天數會超過 999：

```
熟白蝦 26/30  庫存 1,478 ÷ 月銷 21 → 2,111 天  → 被誤判成「沒賣」
```

改用獨立旗標 `isDead`，天數改用 `null` 表示無值。

> **用特殊值當哨兵（999、-1、0）在資料範圍會超過它時必定出錯。**

### 4.2 門市資料不同源

門市走 POS、倉庫走 ERP，兩套系統。銷量只來自 ERP（不含門市），若庫存把門市算進來，**分母含門市、分子不含**，周轉天數被系統性高估：

| 情境 | 含門市 | 不含門市 | 高估 |
|------|--------|---------|------|
| 倉 200／門市 150 | 12 天 | 7 天 | 71% |
| 倉 30／門市 400 | 215 天 | 15 天 | **1333%** |

V24.6 起庫存排除門市庫，且移除「門市專賣」開關——開啟後看到的數字是錯的，留著等於提供一條走向錯誤答案的路。

### 4.3 從三級分類改為積壓金額

原本的 `fast` / `slow` / `overstock` 標準互相矛盾（`slow` 需同時滿足天數與月銷、`overstock` 只看天數），但兩者的處置其實一樣。

改為單一門檻（90 天）＋ **依積壓金額排序**：

```
積壓金額 = 庫存件數 × 單價
單價來源：近三月成交 → 全期間成交 → 報價表（三層退回）
```

三層退回是必要的——**完全沒賣的品項算不出單價，積壓金額會是 0，在依金額排序時被推到最後，與這一頁的目的正好相反。**

### 4.4 季節性品項（V26.2）

| 族群 | 品項 | 旺季 |
|------|------|------|
| 中秋 | 魷魚一夜干、魷魚串（全規格） | 8–9 月 |
| 過年 | 熟白蝦 16/20、21/25、26/30、31/35 | 11–1 月 |

旺季前庫存偏高是**刻意備貨**，不是滯銷。品項仍留在清單上（標示季節），但不計入滯銷統計，天數也不上警示色。

### 4.5 視覺統一

八頁改為同一套：摘要用細線分隔的數字（取代彩色卡）、清單卡片白底加左側 6px 狀態線、數字 `tabular-nums` 對齊、排序與篩選收斂。

**配色原則**：

| 用途 | 顏色 |
|------|------|
| 主文字／數字 | `gray-900` |
| 次要文字 | `gray-400` |
| 選中狀態 | `gray-900` 底白字 |
| 危險 | `red-600` |
| 警示 | `amber-500` |
| 成長／衰退（年度比較專用） | `emerald-600` / `red-600` |

紫色、綠色、藍色作為裝飾用途全部退場。**紅色與琥珀色保留語意，不隨品牌色改變。**

### 4.6 資料來源標示

每頁都在程式註解與畫面上標明來源：

| 分析頁 | 資料來源 |
|--------|---------|
| 庫存周轉 | 庫存表 ＋ 商品銷貨期報表（最近三個月） |
| 每日銷售 | 銷貨日報 `/salesDaily`（GAS 每日同步，含門市） |
| 應收帳款 | 未結案應收明細表 |
| 交叉查詢 | 銷貨明細（不含門市） |
| 流失預警 | 客戶資料 ＋ 銷貨明細推算的購買週期 |
| ABC 分析 | 銷貨明細 |
| 年度比較 | 商品銷貨期報表（年度） |

**目的**：同一品項在不同頁面數字不同時，可立即分辨是「資料源不同」還是「算錯了」。

---

## 5. 實測回報修正

以下皆為使用者實際操作後回報，靜態檢查無法發現。

| 問題 | 原因 | 版本 |
|------|------|------|
| 彈窗底部按鈕看不到 | 底部導航 `z-120` 壓在 11 個彈窗（`z-100`/`z-110`）之上 | V23.5 |
| 存圖片點了沒反應 | 資料是 PNG 但檔名 `.jpg`，iOS Safari 忽略副檔名不符的下載 | V26.0 |
| 存 PDF 右側留白 92mm | 頁面 A4 橫式 297mm 卻只放 200mm 寬的圖 | V26.0 |
| 進報價頁需捲動 | 客戶資訊卡 260px ＋ 空狀態 300px，合計超出可視高度 | V26.7 |
| 搜尋列被鍵盤推走 | `autoFocus` 在排版完成前喚起鍵盤，瀏覽器捲動頁面 | V27.0 |
| 箱容稽核誤判 | 容差僅 0.01，箱容 12 對上實際 11.9 就算不一致 | V26.8 |

**箱容稽核容差**改為 `max(0.05, 箱容 × 3%)`，並將容差內但不完全相等者標示「近似」：

| 情境 | 舊 | 新 |
|------|-----|-----|
| 12 vs 11.9 | 不一致 | **一致（近似）** |
| 30 vs 29.57 | 不一致 | **一致（近似）** |
| 8 vs 19 | 不一致 | 不一致 ✅ |
| 1 vs 18 | 不一致 | 不一致 ✅ |

---

## 6. 檢討：這一輪暴露的問題

### 6.1 靜態檢查不等於正確

庫存周轉月銷高估 20 倍的版本，**通過了 Babel 編譯、作用域檢查、TDZ 檢查**——那些只證明程式跑得起來，不證明算得對。

驗證方法其實極簡單：**打開兩個頁面看同一個品項**。報價頁的庫存、品項速查的件數、庫存周轉的月銷擺在一起，20 倍的落差不可能看不出來。

**已列為交付前必做**：涉及數字的改動，必須用實際資料交叉比對。

### 6.2 程式註解可能是錯的

`d.qty` 那行註解寫著「銷貨包裝數量（件）」，實際是盒數。**依註解判斷欄位意義，直接導致單位錯誤。**

原則：欄位意義以原始報表的欄位定義為準，不以程式註解為準。

### 6.3 「同一件事寫在兩個地方」重複出現

這一輪至少遇到五次：

| 項目 | 症狀 |
|------|------|
| 領料解析器 ×2 | 改一支，另一支照舊丟棄重量 |
| 銷貨解析器 ×3 | 件數欄位有無不一致 |
| 傳 PDF / 存 PDF | 動態頁面尺寸只改了其中一個 |
| 倉別排除規則 | 報價頁完整，庫存周轉自己寫一套 |
| 價格正規化 ×4 | 四個匯出出口各一份（V24.5 已收斂） |

**根因是沒有機制阻止同一件事被寫第二次。** 22,000 行單一元件是溫床。

### 6.4 批次字串替換的風險

大範圍替換在這一輪失手三次：

- `purple` 被連鎖替換成 `graple`（無效的 CSS class）
- 排序區塊截取行時漏掉 `.map(` 那行，結構斷裂
- 價格收斂誤判「六處相同」，實際結構有異，造成變數重複宣告

**兩者皆由編譯檢查攔下並還原**，但顯示批次替換前必須先讀完整個區塊，不能只比對片段。

### 6.5 註解放錯位置

`{條件 && (` 與 JSX 之間插入註解會造成語法錯誤，這一輪犯了三次。

### 6.6 未實測就宣稱修好

搜尋列被鍵盤推走的問題，第一次以為改 CSS 單位（`vh` → `dvh`）就解決，實際上真正的原因是 `autoFocus` 的時序。**在無法實測的環境下，不應宣稱問題已解決。**

---

## 7. 版本更新記錄

### 調撥單（V21.0–V24.0）

| 版本 | 內容 |
|------|------|
| V21.0 | 電腦版導航統一：標題固定品牌名，順序改為首頁·報價·客戶·調撥·分析·管理 |
| V21.1 | 備註展開後自動聚焦；LINE 分享逐步錯誤回報 |
| V21.2 | 挑貨彈窗層級提高（原被底部導航遮蔽） |
| V21.3 | 傳真友善：去灰底、加粗框線、字級放大 |
| V21.4 | 改為 A4 直式 |
| V21.5 | 四欄合併（品名/規格/批號同欄）、字重降為 700–800 |
| V21.6 | 產圖改 PNG 無損、PDF 格式參數修正 |
| V21.7 | **真正載入思源黑體**、等字型就緒才截圖、關閉字型平滑 |
| V21.8 | **改回橫式**，字級再放大 |
| V21.9 | 規格升為主要資訊 |
| V22.0–22.1 | 品名／規格／批號同級 |
| V22.2 | 等比字級階梯（比例 1.25），取代十三種零散字級 |
| V22.3 | 產圖倍率提高 |
| V22.4 | **改傳 PDF**（LINE 傳圖片會重新壓縮） |
| V22.5 | 自動計算安全倍率（iOS canvas 上限 4096px） |
| V22.6 | 二值化 ＋ PDF 串流壓縮（檔案數十 MB → 0.4 MB） |
| V22.7 | 以 B5 實際尺寸回推 300 DPI |
| V22.8 | 備註欄改為有內容才顯示 |
| V22.9 | 品名／規格／批號三行同級 39px |
| V23.0 | 標題置中放大、左右留白 9% → 3.4% |
| V23.1–23.2 | 頁面尺寸依內容比例決定（不再固定 A4） |
| V23.3–23.4 | 開單人員（後撤回）→ 固定聯絡人 |
| V23.5 | **11 個彈窗層級提高**（全部被底部導航遮蔽） |
| V23.6 | 備註欄恢復為一律顯示 |
| V23.7 | 品項速查在分析中心改為一般入口卡 |
| V23.8–23.9 | 備註字級提升至與品名同級 |
| V24.0 | 表頭改為「品名 / 規格 / 批號」 |

### 報表解析（V24.1–V24.5）

| 版本 | 內容 |
|------|------|
| V24.1 | **商品銷貨期報表補上件數欄位**，件數不再依賴箱容推導 |
| V24.2 | 領料明細第二支解析器補上重量欄位、欄位改完全比對 |
| V24.3 | 銷貨明細補上件數欄位 |
| V24.4 | **批次匯入接回入口**（原本無法觸發） |
| V24.5 | 價格正規化四處收斂為單一函式 |

### 分析中心（V24.6–V26.6）

| 版本 | 內容 |
|------|------|
| V24.6 | 庫存周轉排除門市庫與不可售倉 |
| V24.7 | 三級分類改為積壓金額排序 |
| V24.8 | **修正月銷高估 20 倍**、庫存與報價頁對齊 |
| V24.9 | 月份取樣改為「有實際資料的最近三個月」 |
| V25.0 | 標示實際採用的月份 |
| V25.1 | 單價三層退回（近三月 → 全期間 → 報價表） |
| V25.2 | 每日銷售控制項分層、KPI 六張改三個 |
| V25.3 | 庫存周轉改固定欄寬格線 |
| V25.4 | 應收帳款重寫（320 → 168 行） |
| V25.5–25.8 | 交叉查詢單位標示修正、配色統一 |
| V25.9 | 字級層級校準 |
| V26.1 | **修正「沒賣」誤判**（999 哨兵值） |
| V26.2 | 季節性品項處理 |
| V26.3 | 狀態線 3px → 6px |
| V26.4–26.5 | 各頁標明資料來源 |
| V26.6 | 流失預警、ABC 分析、年度比較視覺統一 |

### 實測修正（V26.0、V26.7–V27.0）

| 版本 | 內容 |
|------|------|
| V26.0 | 存圖片副檔名修正、存 PDF 頁面尺寸修正 |
| V26.7 | 報價頁進入不需捲動（客戶卡 260→88px、空狀態 300→96px） |
| V26.8 | 箱容稽核容差 `max(0.05, 3%)` |
| V26.9–27.0 | 搜尋列被鍵盤推走（`dvh` ＋ 延遲 `preventScroll` 聚焦） |

---

## 8. 待處理事項

### 8.1 尚未驗證（優先）

| 項目 | 驗證方式 |
|------|---------|
| **商品銷貨期報表匯入** | 匯入任一月 → 品項速查「詳細」看「報表件數」是否 > 0 |
| **領料明細匯入** | 同上，看「領料實算」是否 > 0 |
| 調撥單傳真效果 | 產一張、存 PDF、印出後傳真給凍庫 |
| 品項主檔 | 尚未匯入 |
| 未結案應收總額 | 應為 33,775,041 |
| `auditSalesDaily` | 稽核腳本尚未執行 |

> V24.1–V26.6 的修正**沒有資料就完全看不出效果**。這是目前最該做的事。

### 8.2 待討論

| 項目 | 說明 |
|------|------|
| **配色方案** | 已提供 13 個方案預覽，尚未選定。需先將 449 處色彩類別抽為 CSS 變數 |
| **採購對照表** | 資料層已備好（`packFromUsage`、`avgWeightPerPiece`），介面未做 |
| **採購計劃演算法** | 現行為「安全庫存 − 現有庫存」，未使用前置期與目標天數；且模型應改為批次進口 |
| 效期風險 | 唯一無人負責的問題：「這批貨在過期前賣得完嗎」 |
| 季節性品項維護 | 目前寫死在程式（`SEASONAL_GROUPS`），若需常增減應改為設定頁 |

### 8.3 權限與資安

四五人使用，暫不做個人帳號。但以下應處理：

| 項目 | 狀態 |
|------|------|
| **登入頁顯示密碼提示** | 「測試密碼：Admin(8888) / Sales(1688)」仍在畫面上，應移除 |
| 密碼強度 | 四位數字，建議改為六至八位混英數（需同步改 Firebase 帳號） |
| **Firebase 安全規則** | 未確認。若為「已登入即可讀寫全部」，業務帳號在技術上可存取應收帳款 |

### 8.4 程式債

| 項目 | 現況 |
|------|------|
| 單一元件 | **22,032 行、220 個 useState** |
| `isMobile` | 定義 8 次，渲染時求值，視窗縮放不重算 |
| 22 個彈窗 | 各自管理開關與關閉邏輯 |
| 分析中心導航 | 未整併至 `DesktopNav` |

---

*本文件由 Claude AI 協助生成，版權歸八方環球所有。*
